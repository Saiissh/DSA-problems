 int s=0,e=arr.length-1,ans=0;
       while(s<=e)
       {
           int mid=(s+e)/2;
// this length checking condition is added in order to break the loop
// when it breaks the boundary conditions
           if(mid+1<arr.length &&  arr[mid]<arr[mid+1])
           {
               ans=mid+1;
               s=mid+1;
           }
           else if( mid+1<arr.length &&  arr[mid]>arr[mid+1])
           {
               ans=mid;
               e=mid-1;
           }
           else 
           break;
       }
       return ans;
